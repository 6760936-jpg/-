"use client";
import Link from "next/link";
import { useCart } from "@/context/CartContext";
import { ProductImage } from "@/components/ProductImage";
import { formatCurrency } from "@/lib/format";
export default function CartPage(){const{items,totalItems,totalPrice,removeItem,setQuantity,clearCart}=useCart();return <div className="container-page min-h-[70vh]"><div className="mb-8"><p className="eyebrow">Ваш заказ</p><h1 className="mt-2 text-3xl font-semibold">Корзина</h1></div>{items.length===0?<div className="surface-card border-dashed p-12 text-center"><h2 className="text-2xl font-semibold">Корзина пока пуста</h2><p className="mt-2 text-zinc-500">Добавьте товары из каталога.</p><Link href="/catalog" className="button-primary mt-6">Перейти в каталог</Link></div>:<div className="grid gap-8 lg:grid-cols-[minmax(0,1fr)_360px]"><div className="space-y-4">{items.map(({product,quantity})=><article key={product.id} className="surface-card grid gap-4 p-4 sm:grid-cols-[110px_1fr_auto] sm:items-center"><ProductImage src={product.image} alt={product.name} className="aspect-square w-full rounded-xl"/><div><span className="text-xs font-semibold text-violet-700">{product.category.name}</span><h2 className="mt-1 font-semibold">{product.name}</h2><p className="mt-1 text-xs text-zinc-500">{product.article} · минимум {product.minOrder} шт.</p><button className="mt-3 text-sm font-semibold text-rose-600" onClick={()=>removeItem(product.id)}>Удалить</button></div><div className="flex items-center justify-between gap-4 sm:block sm:text-right"><div className="inline-flex items-center rounded-xl border border-zinc-300"><button className="size-10 font-semibold hover:bg-zinc-100" onClick={()=>setQuantity(product.id,quantity-1)}>−</button><input className="w-16 border-x border-zinc-300 py-2 text-center text-sm font-semibold outline-none" type="number" min={product.minOrder} max={product.stock} value={quantity} onChange={e=>setQuantity(product.id,Number(e.target.value))}/><button className="size-10 font-semibold hover:bg-zinc-100" onClick={()=>setQuantity(product.id,quantity+1)}>+</button></div><strong className="block text-lg sm:mt-3">{formatCurrency(product.price*quantity)}</strong></div></article>)}<button className="text-sm font-semibold text-zinc-500 hover:text-rose-600" onClick={clearCart}>Очистить корзину</button></div><aside className="surface-card h-fit p-6"><h2 className="text-xl font-semibold">Итого</h2><div className="mt-6 space-y-3 border-b border-zinc-200 pb-5 text-sm"><div className="flex justify-between text-zinc-600"><span>Количество</span><span>{totalItems} шт.</span></div><div className="flex justify-between text-zinc-600"><span>Доставка</span><span>По маршруту</span></div></div><div className="flex items-end justify-between pt-5"><span className="font-semibold">Сумма</span><strong className="text-2xl font-semibold">{formatCurrency(totalPrice)}</strong></div><Link href="/checkout" className="button-primary mt-6 w-full">Оформить заказ</Link><Link href="/catalog" className="button-secondary mt-3 w-full">Продолжить покупки</Link></aside></div>}</div>}
